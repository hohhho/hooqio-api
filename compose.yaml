services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: demodb
      POSTGRES_USER: hooqio
      POSTGRES_PASSWORD: "hooqio1234"
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
